<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.nalan.mdvr2.dao.UserDao">
    <insert id="insertUser" parameterType="com.nalan.mdvr2.entity.User"
    useGeneratedKeys="true" keyProperty="userId" keyColumn="userId">
        INSERT INTO t_user(userId, userType, userState, userName, password, createTime, lastLogin)
        VALUES (#{userId}, #{userType}, #{userState}, #{userName}, #{password}, #{createTime}, #{lastLogin})
    </insert>
</mapper>